{
  "name": "SF6 写作智能体 Crew",
  "agents": ["data_analyst", "argument_builder", "tech_writer"],
  "tasks": [
    {
      "name": "analyze_simulation",
      "description": "使用 FileReadTool 读取 data/qe_results.md（QE 仿真结果摘要）。提取材料信息、计算设置、关键物理量对照表中的全部数值。逐项对照本项目材料与对比材料，输出完整分析：1) 计算设置严谨性说明；2) 关键物理量对照表（物理量、本项目材料数值、对比材料数值、差异量、单位）；3) 每个数值的物理意义解释（如吸附能数值对应化学吸附还是物理吸附、电荷转移方向意味着什么）；4) 数据可靠性评估（哪些差异具有物理显著性，哪些项数据未提供）。注意：表中留空或标注'未提供'的项必须在分析中如实标注为'未提供'，严禁编造数值。",
      "expected_output": "一份 Markdown 格式的结构化分析文档，包含：计算设置说明、关键物理量对照表（含差异与单位）、逐项物理解释、数据可靠性评估与缺失项清单。",
      "agent": "data_analyst"
    },
    {
      "name": "build_arguments",
      "description": "基于仿真分析结果，构建论证本项目材料优于对比材料的论点框架。要求：1) 提炼 3-5 条核心优势论点，每条必须包含完整链路：数据依据（引用具体数值）→ 物理机制（该数值为何导致该效应）→ 性能优势（对灵敏度/选择性/可恢复性/稳定性的实际影响）；2) 如数据中有干扰气体吸附能，必须构建选择性论证；3) 数据上处于劣势或未提供的项，给出诚实的技术权衡说明或明确回避，不回避时给出合理解释；4) 严格遵守分析结果中'队友备注'的红线要求；5) 所有数值必须与第一步分析结果完全一致，不得引入外部数值。",
      "expected_output": "3-5 条结构化论点框架（Markdown），每条论点含'数据依据/物理机制/性能优势'三段，另附劣势项权衡说明表。",
      "agent": "argument_builder",
      "context": ["analyze_simulation"]
    },
    {
      "name": "write_report",
      "description": "将论点框架撰写成完整的中文科技论述文稿，用于挑战杯申报材料。结构要求：1) 开头段：项目背景引入与核心结论概述（一两句话点明本项目材料为何更优）；2) 关键物理量对比表（Markdown 表格，含单位）；3) 分点论证：每个论点独立成段，采用'提出观点—数值证据—物理解释—对比结论'结构；4) 劣势与权衡说明段（如论点框架中有）；5) 总结段：综合论证材料体系的优越性并展望应用。写作规范：语言正式客观，避免绝对化措辞（用'显著优于'而非'完美'），正确使用单位（eV、e、Å），化学式规范书写（如 SOF₂、SO₂F₂、SF₆），不得引用论点框架之外的任何数值。",
      "expected_output": "一份完整的中文学术论述文稿（Markdown），800-1500 字，含对比表格、分点论证、总结段落，语言规范可直接用于申报材料。",
      "agent": "tech_writer",
      "context": ["analyze_simulation", "build_arguments"],
      "output_file": "output/论证报告.md",
      "markdown": true
    }
  ],
  "process": "sequential",
  "verbose": false
}
